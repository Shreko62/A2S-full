{% extends 'base.html.twig' %}

{% block title %}Situation index{% endblock %}

{% block body %}
    <h1>Situation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Professionnelle</th>
                <th>Entreprise</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for situation in situations %}
            <tr>
                <td>{{ situation.id }}</td>
                <td>{{ situation.Professionnelle }}</td>
                <td>{{ situation.Entreprise }}</td>
                <td>
                    <a href="{{ path('situation_show', {'id': situation.id}) }}">show</a>
                    <a href="{{ path('situation_edit', {'id': situation.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('situation_new') }}">Create new</a>
{% endblock %}
